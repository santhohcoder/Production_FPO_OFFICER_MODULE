package com.demo.fpo.repos;

import org.springframework.data.jpa.repository.JpaRepository;

import com.demo.fpo.model.REALLOCATION;

public interface Reallocation_repo extends JpaRepository<REALLOCATION, Long> {

}
